# Pure-JAX P2G with lax.scan over the 27 stencil offsets.
# Avoids the (N, 27, *) intermediate that the default jax path materialises,
# but still pays 27 small XLA kernel launches per substep -- slower than
# cuda_fused (which keeps the 27-loop in registers), faster than jax.
name: jax_v1_5
